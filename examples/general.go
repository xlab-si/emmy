package examples

import (
//"github.com/xlab-si/emmy/commitments"
//"log"
//"math/big"
)

func General() {

	/*client := commitments.NewClient("pedersen")

	//commitVal := big.NewInt(121212121)
	success, err := client.Execute()

	if err != nil {
		log.Printf("Error: %v", err)
	}

	if success == true {
		log.Println("ok")
	} else {
		log.Println("not ok")
	}
	*/
}
